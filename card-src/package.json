{
  "name": "webasto-connect-card",
  "private": true,
  "version": "0.1.0b6",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "build:watch": "node build.mjs --watch"
  },
  "devDependencies": {
    "esbuild": "^0.25.2"
  }
}
